"use client";

import Grid from "rsuite/Grid";
import Row from "rsuite/Row";
import Col from "rsuite/Col";
import Center from "rsuite/Center";

/**
 * Re-export RSuite Grid system
 * AppGrid, AppRow, AppCol = UI abstraction layer
 */

export const AppGrid = Grid;
export const AppRow = Row;
export const AppCol = Col;
export const AppCenter = Center;
